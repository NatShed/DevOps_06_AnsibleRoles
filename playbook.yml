---
- name: Install nginx vhost
  hosts: webservers
  remote_user: runner1
  become: true
  become_method: sudo
  gather_facts: false

  vars: 
    sites:
    - "fizmat.ru"
    - "ibas.ru"
    - "nat.com"
  
  roles:
  - nginx-vhosts
